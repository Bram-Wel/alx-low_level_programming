#include "lists.h"
#include <stdio.h>

/**
  * main - Starting point.
  *
  * Description: Run with cmd ./pl.x > file, to save to file.
  * Return: 0.
  */
int main(void)
{
	printf("%d", large_palindrome(3));
	return (0);
}
