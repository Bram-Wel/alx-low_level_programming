#include "main.h"

/**
  * _strlen - Find length of a string.
  * @s: String to evaluate.
  *
  *Return: Length of the string.
  */
int _strlen(char *s)
{
	char array[10000];
	int i;
	int Strlen;

	array = *s

	i=0;
	while (array[i] != '\0')
	{
		Strlen = i;
		i++;
	}

	return (Strlen);
}
